# Generic AI Ops Example

This example will become the reviewer-safe sample app for the template. It uses
fictional workspaces, synthetic sources, fake provider events, and generic
workflow recipes to demonstrate the reusable framework without product-specific
business logic.
