# GTM Context App Example

This example will demonstrate how the template can be adapted into a
go-to-market context engineering app. It is an example package, not the core
template domain, and all seed data must remain synthetic.
